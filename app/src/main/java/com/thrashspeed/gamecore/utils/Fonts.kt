package com.thrashspeed.gamecore.utils

import androidx.compose.ui.text.font.Font
import androidx.compose.ui.text.font.FontFamily
import com.thrashspeed.gamecore.R

object Fonts {
    val galactica: FontFamily by lazy {
        FontFamily(
            Font(R.font.galactica)
        )
    }

    val dpcomic: FontFamily by lazy {
        FontFamily(
            Font(R.font.dpcomic)
        )
    }

}